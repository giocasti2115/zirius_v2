services:
  app:
    image: php:8.2-cli
    container_name: zirius_app
    working_dir: /var/www
    volumes:
      - ./:/var/www
    command: php artisan test

networks:
  zirius_network:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 172.28.0.0/16